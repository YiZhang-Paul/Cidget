enum NotificationType {
    Commit = 'commit',
    PullRequest = 'pull-request',
    CiBuild = 'ci-build',
    CdRelease = 'cd-release',
    SupportTicket = 'support-ticket'
}

export default NotificationType;
