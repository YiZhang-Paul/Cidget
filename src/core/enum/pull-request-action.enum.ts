enum PullRequestAction {
    Opened = 'opened',
    Updated = 'updated',
    Approved = 'approved',
    Rejected = 'rejected',
    Merged = 'merged',
    ReviewRequested = 'needs review'
}

export default PullRequestAction;
